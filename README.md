# StrangeEngine
A Library of scripts to be used with unity to make game development easier.

the StrangeEngine is not a standalone engine (yet) so it is to be used in conjunction with unity.

importing the engine may be problemnatic at first, just search through the scripts for "IMPORT::" and you will find a comment telling you how to properly attach that class to unity so it will run as expected.
